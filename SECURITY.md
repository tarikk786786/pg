# Security notice

Credentials and production data found in the original archive were removed before this repository was published.

- Replace every `CHANGE_ME` placeholder through secure deployment configuration.
- Do not commit live database passwords, API tokens, private keys, or production database records.
- Rotate the credentials that were present in the original archive.
